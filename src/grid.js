const FRAME_RATE = 8; // Frames per second

export class Grid {
    // Constructor
    constructor(rows = 0, cols = 0) {
        this.rows = rows;
        this.cols = cols;
    }

    // Draws the grid
    draw() {
        // TODO 
    }
}